# Stripez environment
# Copy to .env and adjust values as needed.

# App
PORT=8080
SESSION_SECRET=change-me
CORS_ORIGINS=https://nicat.mteij.nl,https://schikko-rules.web.app,https://schikko-rules.firebaseapp.com,http://localhost:8080

# Oracle (Gemini)
GEMINI_KEY=
ORACLE_MODEL=gemini-2.5-flash

# Database (used by server)
DATABASE_URL=postgres://stripez:stripez@postgres:5432/stripez

# Postgres service (used by docker-compose)
POSTGRES_USER=stripez
POSTGRES_PASSWORD=stripez
POSTGRES_DB=stripez