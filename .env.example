# Stripez environment
# Copy to .env and adjust values as needed.

# App
PORT=8080
CORS_ORIGINS=https:/my-domain.com

APP_NAME=Bucom
APP_YEAR=2026

# Oracle (Gemini)
GEMINI_KEY=
ORACLE_MODEL=gemini-flash-latest

# Stripez event configuration
# Default duration (in days) used when no duration is provided via UI. Minimum 1.
STRIPEZ_DEFAULT_DURATION_DAYS=3
# After the event ends, wait this many hours before auto-unsetting the Schikko (and performing cleanup).
STRIPEZ_UNSET_DELAY_HOURS=6
# What to do at that moment. One of:
#   NUKE               -> remove people, stripes, rules, logbook (everything)
#   NOTHING            -> keep everything
#   KEEP_DECREES       -> delete people & stripes, keep rules
#   KEEP_LEDGER        -> delete rules, keep people & stripes
#   REMOVE_STRIPES_ONLY-> delete only stripes
STRIPEZ_CLEANUP_ACTION=NOTHING

# Drinking policy (default: require Schikko approval for guests' drunk stripes)
# true  -> guests submit requests; Schikko must approve
# false -> guests can record drunk stripes themselves (subject to event timing rules)
DRINK_REQUIRE_APPROVAL=true

# SQLite database file
DB_FILE=../data/stripez.sqlite

# Admin break-glass key (optional). When set, you can log in by entering this key instead of a TOTP code.
ADMIN_KEY=